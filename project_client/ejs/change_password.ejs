<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <!-- <link rel="stylesheet" href="/change-password.css"> -->
     <link rel="stylesheet" href="/edit-profile.css">
</head>
<body>
    <div id="main">
        <div id="top-bar">
            <div id="heading">
                Data<span>Nest</span>
            </div>

            <div id="user">
                <div class="user-avatar" id="user-toggle">
                    <%= username.charAt(0).toUpperCase() %>
                </div>

                <div id="user-name">
                    <%= username.charAt(0).toUpperCase() + username.slice(1) %>
                </div>

                <!-- Dropdown -->
                <div id="user-dropdown" class="hidden">
                    <div class="dropdown-item">🖥️ <a href="/startpage">Workspace</a></div>
                    <div class="dropdown-item logout">🚪 <a href="/login">Logout</a></div> 
                    <!-- //employ the cache deleting logic again -->
                </div>
            </div>
            <script>
                const userToggle = document.getElementById("user-toggle");
                const dropdown = document.getElementById("user-dropdown");

                userToggle.addEventListener("click", (e) => {
                    e.stopPropagation();
                    dropdown.classList.toggle("hidden");
                });

                // Close on outside click
                document.addEventListener("click", () => {
                    dropdown.classList.add("hidden");
                });
            </script>

        </div>
        <div class="profile-page">
            <div class="profile-card">
                <div class="profile-header">
                    <div class="avatar">
                        <%= firstname[0].toUpperCase() %><%= lastname[0].toUpperCase() %>
                    </div>
                    <div class="profile-info">
                        <h2><%= firstname %> <%= lastname %></h2>
                        <p class="username">@<%= username %></p>
                    </div>
                </div>

                <div class="profile-section">
                    <form action='/change_password' id="change_password" method='POST'>
                        <input type="hidden" name="firstname" value="<%= firstname %>">
                        <input type="hidden" name="lastname" value="<%= lastname %>">
                        <input type="hidden" name="username" value="<%= username %>">
                        <input type="hidden" name="password" value="<%= password%>">
                        <div class="profile-edit-row">
                            <span class="label">New Password</span>
                            <input type='password' class='profile-input' name='newpassword' value='' required>
                        </div>

                        <div class="profile-edit-row">
                            <span class="label">Confirm New Password</span>
                            <input type='password' class='profile-input' name='confirmnewpassword' value='' required>
                        </div>
                    </form>
                </div>

                <div class="profile-actions">
                    <button class="btn secondary" type="submit" form="change_password" name="submit" value="cancel">Cancel</button>
                    <button class="btn submit" type="submit" form="change_password" name="submit" value="change">Change</button>
                </div>
            </div>

        </div>
    </div>
</body>
</html>